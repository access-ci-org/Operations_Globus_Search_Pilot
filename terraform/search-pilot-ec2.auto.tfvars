# Name of the keypair on AWS
key_pair = "accessops-aws-root"

# Instance type, if not set will default to t4g.micro
instance_type = "t4g.medium"

# Instance elastic IP
elastic_ip_search_pilot = "eipalloc-0be6802cf8b3fad80"
